export default function FinalCTA() {
  return (
    <section id="cta" className="landing-section final-cta">
      <h2 className="landing-title reveal">
        Bereit für ein System, das Kunden bringt?
      </h2>
      <p className="reveal" style={{ color: 'var(--landing-muted)', maxWidth: '52ch', margin: '0 auto 1.25rem' }}>
        Wir bauen Webseite, Funnel und Automation als ein zusammenhängendes System.
        Keine Templates, keine Copy-Paste-Stacks — nur das, was bei dir tatsächlich Umsatz produziert.
      </p>
      <a href="/contact" className="hero-cta reveal">Projekt besprechen</a>
    </section>
  );
}
